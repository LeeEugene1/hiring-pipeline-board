# 채용 파이프라인 보드 작업 지침

이 저장소는 프론트엔드 채용 과제를 수행하기 위한 전용 저장소다. 구현 속도보다 요구사항 분해, AI 출력 검증, 기능 단위 커밋 기록을 중요하게 다룬다.

## 기본 원칙

- 모든 사용자 안내, 문서 설명, 커밋 제목과 본문은 한글로 작성한다.
- 라이브러리명, 파일명, 코드 식별자, 커밋 타입과 범위는 원래 표기를 유지한다.
- `main` 브랜치에 직접 커밋하거나 푸시하지 않는다.
- 기능 하나마다 별도 브랜치를 만들고 Pull Request를 통해 병합한다.
- 의존성이 없고 수정 파일이 겹치지 않는 기능은 별도 worktree에서 병렬로 진행할 수 있다.
- 기능 하나를 구현하고 검증한 뒤 하나의 커밋으로 남긴다.
- 기능 커밋과 같은 커밋에 `PROMPTS.md`의 대응 섹션을 포함한다.
- 중요한 설계 판단이 생긴 경우에만 `DECISIONS.md`를 갱신한다.
- `README.md`에는 실제로 동작하고 검증한 기능만 기재한다.
- 커밋을 합치거나 기존 시행착오 기록을 삭제하지 않는다.
- 사용하지 않는 코드, 불필요한 의존성, 서로 모순되는 구현 패턴을 남기지 않는다.

## 기술 기준

- 애플리케이션: React, TypeScript, Vite
- 서버 상태: TanStack Query
- Mock API: MSW
- 테스트: Vitest, Testing Library
- 스타일: Tailwind CSS
- UI Primitive: 실제 필요한 경우에만 shadcn/ui 컴포넌트를 선택적으로 추가
- 클라이언트 상태: React `useState`, 필요 시 `useReducer`
- 아이콘: Lucide React

후보자 데이터와 API 상태를 별도 전역 Store에 복제하지 않는다. 다른 라이브러리를 추가할 때는 기존 도구로 해결할 수 없는 이유를 먼저 확인한다. 핵심 상태 로직을 대신하는 라이브러리를 추가한 경우 선택 이유를 `DECISIONS.md`에 기록한다.

## 필수 구현 범위

1. 서류검토, 면접, 처우협의, 최종합격, 불합격 컬럼을 표시한다.
2. 지원자 카드에 이름, 직무, 지원일, 현재 단계를 표시한다.
3. 명시적 액션 버튼으로 지원자의 단계를 이동한다.
4. 단계 변경 내용을 Mock API에 저장하고 새로고침 후에도 유지한다.
5. UI를 먼저 변경하고 API 실패 시 정확한 이전 상태로 복구한다.
6. 실패 시 사용자가 이해할 수 있는 피드백을 표시한다.
7. 이름 부분 검색과 직무 필터를 제공한다.
8. 200건 이상의 데이터에서도 검색과 필터가 체감 지연 없이 동작해야 한다.
9. 카드 클릭 시 지원자 상세 사이드 패널을 연다.
10. 패널 외부와 닫기 버튼 클릭으로 패널을 닫는다. ESC 키 닫기는 구현하지 않는다.
11. 로딩, 오류, 전체 데이터 없음, 검색 결과 없음 상태를 구분한다.
12. MSW 요청에 200~800ms의 무작위 지연과 약 15%의 실패를 적용한다.
13. 평가자가 실패 복구를 재현할 수 있는 강제 실패 방법을 제공한다.

## 기능 작업 절차

각 기능은 아래 순서로 진행한다.

1. 이슈의 선행 작업과 수정 예상 파일을 확인한다.
2. 기준 브랜치와 파일 소유권을 정하고 기능 전용 worktree와 브랜치를 만든다.
3. 현재 코드와 관련 테스트를 읽는다.
4. 이번 PR에서 다룰 기능 하나와 검증 기준을 정한다.
5. 구현 전에 사용자에게 변경 범위를 짧게 알린다.
6. 기능을 구현한다.
7. 정상 흐름, 실패 흐름, 경계 조건을 직접 검증한다.
8. 사용한 실제 프롬프트와 AI 출력 요지를 `PROMPTS.md`에 기록한다.
9. AI 제안에서 수정하거나 기각한 내용과 검증 방법을 기록한다.
10. 설계 결정이 생겼다면 `DECISIONS.md`를 갱신한다.
11. 사용자 실행 방법이나 구현 기능이 달라졌다면 `README.md`를 갱신한다.
12. `./scripts/validate-submission.sh`를 실행한다.
13. 이번 기능과 관련된 파일만 스테이징해 커밋한다.
14. 기능 브랜치를 원격에 푸시하고 `main` 대상 Pull Request를 생성한다.
15. PR에서 변경 범위와 검증 결과를 다시 확인한다.
16. 사용자가 검토를 마치면 merge commit 방식으로 병합하고 최신 기준 브랜치를 받는다.

## 커밋 규칙

커밋 제목은 `type(scope): 한글 요약` 형식을 사용한다.

사용 가능한 타입:

- `feat`: 기능 추가
- `fix`: 오류 수정
- `refactor`: 동작을 바꾸지 않는 구조 개선
- `test`: 테스트 추가 또는 수정
- `docs`: 문서 변경
- `chore`: 개발 환경과 설정 변경

커밋 본문에는 다음 내용을 포함한다.

- 무엇을 변경했는지
- 왜 그렇게 변경했는지
- AI 초안을 어떻게 검토하고 수정했는지
- 대응하는 `PROMPTS.md` 섹션

여러 기능을 한 커밋에 섞지 않는다. `squash`, `rebase`, `force-push`로 과제 진행 기록을 지우지 않는다.

## 브랜치와 Pull Request 규칙

- 저장소 생성에 필요한 최초 커밋을 제외하고 `main` 직접 커밋을 금지한다.
- 기능 하나를 브랜치 하나와 PR 하나로 관리한다.
- 브랜치 이름은 `feature/board-layout`, `fix/stage-rollback`, `docs/pr-workflow`처럼 작성한다.
- PR 제목은 커밋과 같은 `type(scope): 한글 요약` 형식을 사용한다.
- PR 본문에는 변경 요약, 검증 결과, AI 제안에서 수정한 내용을 한글로 작성한다.
- PR에는 관련 없는 기능이나 문서 변경을 섞지 않는다.
- 리뷰 중 발견한 문제는 같은 브랜치에 별도 수정 커밋으로 남길 수 있다.
- 과제의 기능별 커밋 기록을 보존하기 위해 `Squash and merge`와 `Rebase and merge`를 사용하지 않는다.
- PR은 `Create a merge commit` 방식으로 병합한다.
- PR 승인과 병합은 사용자가 직접 수행한다.
- 병합 후 `main`을 최신 상태로 갱신하고 아직 진행 중인 브랜치의 기준 상태를 확인한다.

## 병렬 작업 규칙

- 메인 에이전트가 전체 이슈 의존 관계와 통합 순서를 관리한다.
- 의존성이 없고 수정 예상 파일이 겹치지 않는 이슈만 병렬 개발한다.
- 병렬 작업마다 독립된 git worktree와 기능 브랜치를 사용한다.
- 하나의 서브에이전트는 하나의 worktree와 명시된 소유 파일만 다룬다.
- 서브에이전트는 다른 worktree, `main`, 다른 기능 브랜치를 수정하지 않는다.
- `README.md`, `PROMPTS.md`, `DECISIONS.md`는 메인 에이전트만 수정한다.
- `package.json`, lock 파일, 애플리케이션 진입점, 공통 타입, Query 설정처럼 충돌 가능성이 높은 파일은 동시에 수정하지 않는다.
- 의존성 설치가 필요한 이슈는 한 번에 하나만 통합한다.
- 서브에이전트는 구현 결과, 변경 파일, 테스트 결과, AI 제안 검토 내용을 메인 에이전트에게 반환한다.
- 메인 에이전트는 결과를 검토한 뒤 공통 문서 갱신, 최종 테스트, 커밋, 푸시와 PR 생성을 담당한다.
- 독립된 PR은 동시에 열 수 있지만 공유 파일이나 선행 관계가 있으면 stacked PR로 연결하거나 통합 순서를 기다린다.
- stacked PR은 부모 기능 브랜치를 기준으로 만들고 부모 PR 병합 후 기준 브랜치를 `main`으로 변경한다.
- 병합 전 최신 기준 브랜치를 merge 방식으로 반영하고 충돌을 해결한다. rebase와 force-push는 사용하지 않는다.
- 자동 승인과 자동 병합을 사용하지 않는다. 사용자가 변경사항과 CI 결과를 확인한 뒤 merge commit으로 병합한다.

## `PROMPTS.md` 작성 규칙

- 기능과 커밋 순서에 맞춰 섹션을 추가한다.
- AI에 전달한 주요 프롬프트는 실제 문구를 기록한다.
- AI 출력 전체를 복사하지 않고 결과의 요지만 정리한다.
- 리뷰 및 검증에는 문제, 발견 방법, 채택·수정·기각 판단, 실제 검증 결과를 기록한다.
- 실행하지 않은 테스트나 확인하지 않은 결과를 작성하지 않는다.
- 사소한 오타 수정처럼 기능 판단과 무관한 대화는 생략할 수 있다.

## `DECISIONS.md` 작성 규칙

- 주요 설계 결정은 최종적으로 3~5개를 유지한다.
- 결정마다 배경, 선택지, 최종 결정, 이유와 트레이드오프를 기록한다.
- AI 제안을 채택하거나 기각한 이유를 포함한다.
- 모호한 요구사항에 적용한 가정을 기록한다.
- 완료하지 못한 기능이 있다면 대상, 이유, 대안 또는 후속 구현 방법을 기록한다.

## 검증 기준

- 변경 후 가능한 범위에서 테스트, 린트, 타입 검사, 빌드를 실행한다.
- 낙관적 업데이트는 성공과 실패를 모두 검증한다.
- 실패 복구 테스트는 변경 직전 상태가 정확히 복원되는지 확인한다.
- 같은 카드를 빠르게 연속 이동하는 경우의 응답 순서를 확인한다.
- 키보드만으로 카드의 단계 이동, 상세 열기, 닫기 버튼 접근이 가능해야 한다.
- 검증하지 못한 항목은 완료한 것처럼 표현하지 않고 이유를 보고한다.

## 작업 완료 조건

- 필수 기능이 실제 브라우저에서 동작한다.
- 새로고침 후 변경한 단계가 유지된다.
- 무작위 실패와 강제 실패에서 원상 복구가 동작한다.
- 테스트, 린트, 타입 검사, 빌드가 통과한다.
- 세 제출 문서가 현재 구현과 일치한다.
- 기능별 커밋과 `PROMPTS.md` 기록이 대응한다.
- 모든 기능 변경이 PR을 통해 `main`에 병합되어 있다.
