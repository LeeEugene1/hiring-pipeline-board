# 설계 결정 기록

## 결정 1. 저장소 전용 하네스를 사용한다

### 배경

이번 채용 과제 하나에서만 사용할 작업 절차와 검증 장치가 필요하다.

### 검토한 선택지

- 사용자 전역에서 동작하는 Codex 스킬
- 저장소의 `AGENTS.md`와 검증 스크립트

### 최종 결정

저장소 내부의 `AGENTS.md`와 `scripts/validate-submission.sh`를 사용한다.

### 이유와 트레이드오프

과제 규칙이 저장소와 함께 버전 관리되며 평가자가 작업 기준을 확인할 수 있다. 다른 프로젝트에서 자동으로 재사용되지는 않지만 이번 과제에만 사용하므로 문제가 없다.

### AI 제안 검토

AI는 반복 사용 시 스킬이 적합하다고 제안했지만, 단일 과제라는 범위를 확인한 뒤 저장소 전용 방식으로 조정했다.

### 병렬 작업 정책

독립된 기능은 별도 worktree와 기능 브랜치에서 병렬로 개발할 수 있게 한다. 다만 공통 문서, 의존성 파일, 애플리케이션 진입점과 공통 타입은 메인 에이전트가 단독으로 통합한다. 무제한 병렬 작업보다 파일 소유권과 의존 관계를 먼저 확인하는 통제된 병렬 방식을 선택했다.

### CI/CD 정책

GitHub Actions의 제출 검증이 통과한 경우에만 Vercel CLI로 배포한다. Pull Request는 Preview, `main` push는 Production 환경에 배포하고 포크 Pull Request는 비밀값을 제공하지 않는다. Vercel Git 자동 배포는 같은 커밋의 중복 배포를 막기 위해 Actions 배포 검증 후 해제한다. Vercel에 소스 빌드를 다시 맡기지 않고 Actions에서 생성한 prebuilt 결과만 올리므로 검증과 배포가 같은 소스를 사용하지만, GitHub Actions Secrets와 CLI 버전을 직접 관리해야 한다.

## 결정 2. 클라이언트 애플리케이션에 필요한 도구만 조합한다

### 배경

단일 보드 화면에서 브라우저 API, Mock API와 낙관적 업데이트를 구현해야 한다. 애플리케이션 실행 환경, 스타일링, UI Primitive와 상태 관리 도구의 역할이 겹치지 않게 정해야 한다.

### 검토한 선택지

- Vite + React와 Next.js App Router
- Tailwind CSS 단독 사용과 Tailwind CSS + shadcn/ui 전체 도입
- React 내부 상태와 Zustand 전역 Store

### 최종 결정

React, TypeScript, Vite를 애플리케이션 기반으로 사용한다. Tailwind CSS로 보드와 카드 스타일을 직접 구성하고, shadcn/ui는 상세 패널처럼 접근성 상호작용이 복잡한 컴포넌트만 필요한 기능 이슈에서 추가한다. 후보자 API 상태는 TanStack Query, 검색어·필터·선택 ID 같은 화면 상태는 React `useState`로 관리한다. Zustand는 도입하지 않는다.

### 이유와 트레이드오프

이 과제는 SEO, 서버 렌더링, 서버 컴포넌트와 다중 라우트가 필요하지 않다. 대부분의 기능이 `localStorage`, 이벤트와 클라이언트 상태를 사용하므로 Next.js를 선택하면 Client Component 경계만 늘어난다.

Tailwind CSS는 반응형 보드 레이아웃을 빠르게 조정할 수 있고 스타일 근거가 컴포넌트 가까이에 드러난다. shadcn/ui 전체 도입은 사용하지 않는 생성 코드와 의존성을 늘리므로 기각했다. 대신 포커스 관리가 필요한 UI Primitive만 코드를 검토한 뒤 선택적으로 추가한다.

TanStack Query가 후보자 목록, 로딩·오류, mutation과 rollback의 단일 원본을 담당한다. 같은 후보자 데이터를 Zustand에 복제하면 성공, 실패와 재조회 때 두 상태를 동기화해야 한다. 현재 화면 전용 상태는 가장 가까운 공통 부모의 React 상태로 충분하다. Undo처럼 여러 기능이 공유하는 복잡한 클라이언트 상태가 추가되면 `useReducer`를 먼저 검토하고, 한계가 확인될 때 Zustand를 다시 평가한다.

검색어와 직무도 `CandidatePipelineBoard`의 React 내부 상태로 관리한다. 이름 검색어에만 `useDeferredValue`를 적용하고, API 원본 배열에서 직무 목록과 검색 결과를 `useMemo`로 파생한다. 200건은 별도 검색 인덱스나 전역 Store가 필요한 규모가 아니며, 직무 select는 입력 연속성이 필요하지 않으므로 지연하지 않고 즉시 반영한다. 문자열 정규화와 교집합 계산은 순수 함수로 분리한다.

### AI 제안 검토

AI가 Next.js, Tailwind CSS와 shadcn/ui, Zustand의 장단점을 비교한 결과를 그대로 채택하지 않고 과제의 데이터 흐름과 실제 사용 범위에 대입했다. Next.js 서버 기능, shadcn/ui 전체 설치와 Zustand 후보자 Store는 현재 요구사항에 불필요한 복잡성과 중복 상태를 추가한다고 판단해 기각했다. 검색 성능 접근은 debounce와 deferred value 중 타이머 정리와 테스트 대기가 필요 없는 React 내장 deferred value를 채택하되, 모든 필터 변경을 지연하는 초안은 직무 선택 피드백까지 늦추므로 이름 검색에만 적용하도록 범위를 줄였다.

## 결정 3. MSW와 localStorage로 Mock API를 구성한다

### 배경

실제 HTTP 요청 형태를 유지하면서 200~800ms의 지연, 약 15%의 실패, 새로고침 후 데이터 유지를 구현해야 한다.

### 최종 결정

MSW handler가 localStorage 기반 데이터 저장 계층을 읽고 수정하도록 구성한다.

### 이유와 트레이드오프

UI가 실제 HTTP API와 같은 방식으로 통신하면서도 별도 백엔드 프로세스 없이 실패와 지연을 재현할 수 있다. 인메모리 데이터는 새로고침하면 사라지고 json-server는 실행 프로세스가 하나 더 필요하므로 기각했다.

200명의 seed를 `hiring-pipeline:candidates`에 저장하고 GET과 PATCH handler가 같은 저장 계층을 사용한다. 모든 요청은 200~800ms 지연 후 기본 15% 확률로 실패한다. 평가와 테스트에서 무작위 결과를 제어할 수 있도록 `hiring-pipeline:mock-failure-mode`를 `always`, `never` 또는 미설정 상태로 구분했다. 강제 모드에서도 네트워크 지연은 유지한다.

Mock API는 실제 백엔드를 대신하므로 배포 빌드에서도 기본 활성화한다. 실제 API가 연결되는 환경에서는 `VITE_ENABLE_MSW=false`로 명시적으로 끌 수 있다. 브라우저 저장소는 여러 사용자와 기기 사이에 공유되지 않는 한계가 있지만 이번 단일 사용자 과제 범위에는 적합하다.

### AI 제안 검토

AI가 무작위 함수를 handler에 직접 넣는 초안을 제안했지만 테스트가 간헐적으로 실패하고 평가자가 rollback을 재현하기 어렵다고 판단했다. 지연 범위와 실패 판정을 순수 함수로 분리하고 실패 모드를 요청 쿼리 또는 localStorage에서 결정하도록 수정했다. 저장 데이터가 손상되면 seed로 복구하고 유효하지 않은 단계와 후보자 ID에는 각각 400, 404를 반환하도록 경계 조건도 추가했다.

## 결정 4. 명시적 액션 버튼으로 단계를 이동한다

### 배경

과제는 드래그앤드롭과 명시적 액션 버튼 중 하나를 허용하며 키보드 접근 경로도 평가한다. 이전에 드래그앤드롭을 구현했을 때 잘못된 위치 이동과 화면·저장 상태 불일치 같은 문제를 경험했다. 제한 시간 안에 정상 이동뿐 아니라 네트워크 실패까지 안정적으로 검증할 수 있는 방식이 필요했다.

### 최종 결정

지원자 카드 하단에 명시적인 단계 이동 액션을 제공한다. 가운데에는 Radix UI 기반 selectbox 형태의 단계 메뉴를 두어 모든 단계로 이동할 수 있게 하고, 양옆에는 인접한 이전·다음 단계로 바로 이동하는 왼쪽·오른쪽 버튼을 둔다. 현재 단계는 메뉴에서 체크 표시와 함께 비활성화하며 별도 확인창은 표시하지 않는다.

### 이유와 트레이드오프

명시적 액션은 이동 방향과 요청 시점을 코드와 테스트에서 분명하게 확인할 수 있어 DnD보다 버그 범위가 작다. selectbox 형태의 메뉴는 임의 단계 이동을, 좌우 버튼은 반복적인 인접 단계 이동을 빠르게 처리해 안정성을 위해 DnD를 포기하면서 생기는 조작 비용을 줄인다. Radix UI Primitive가 메뉴의 포커스 이동과 키보드 상호작용을 담당하므로 직접 구현할 때 발생할 수 있는 접근성 누락도 줄인다. 전체 shadcn/ui 컴포넌트 묶음은 설치하지 않는다.

#7에서는 API 저장 경로를 먼저 검증하기 위해 성공 후 화면을 갱신했다. #8에서는 200~800ms 지연 동안의 조작 응답성을 높이기 위해 단계 선택 직후 Query 캐시를 변경하는 낙관적 업데이트를 적용한다. mutation 직전 해당 후보자의 snapshot을 저장하고 실패 시 그 후보자만 복원한다. 전체 배열 snapshot을 그대로 덮지 않으므로 동시에 변경된 다른 후보자 상태를 보존할 수 있다.

낙관적 이동으로 카드 컴포넌트가 다른 컬럼에서 다시 마운트되므로 컴포넌트 로컬 mutation 상태만 사용하면 실패 메시지가 사라질 수 있다. TanStack Mutation Cache에서 후보자별 최신 mutation 상태를 조회해 rollback된 카드에 오류를 표시한다.

동일 후보자를 연속 이동할 때는 후보자별 요청 번호와 `AbortController`를 사용한다. 새 선택이 들어오면 이전 요청을 중단하고 최신 요청 번호와 일치하는 callback만 캐시를 확정하거나 rollback한다. Mock API handler도 지연 후 저장하기 전에 중단 신호를 확인해 오래된 요청이 localStorage를 덮지 않게 한다. 요청을 모두 병렬로 완료시키는 방식보다 이전 요청 결과를 버리지만 마지막 사용자 의도를 화면과 저장소에서 동일하게 유지한다.

### AI 제안 검토

AI 초안은 단계 메뉴만 제공하고 위·아래 순서 변경은 필수 범위가 아니므로 제외하는 방식을 제안했다. 사용자는 단계 메뉴만으로는 반복 이동 UX가 불편하다고 판단해 좌우 인접 단계 버튼을 추가하도록 수정했다. 같은 컬럼 안의 위·아래 카드 순서 변경은 별도 API와 영속화가 필요하고 필수 요구사항이 아니므로 #20 이슈로 분리했다. DnD는 과거 문제 경험과 제한 시간 내 안정성 검증을 근거로 최종 기각했다.

낙관적 업데이트 AI 초안의 전체 후보자 배열 rollback은 다른 후보자의 최신 변경까지 덮을 수 있어 기각했다. 후보자 단위 snapshot 복구로 수정하고, 응답 대기 중 다른 후보자의 캐시를 변경한 뒤 실패시에도 보존되는 테스트를 추가했다.

경쟁 상태 AI 초안에서 오래된 성공 응답을 UI에서만 무시하는 방식은 Mock API 저장값이 이전 단계로 남을 수 있어 기각했다. 클라이언트 요청 중단과 handler의 중단 신호 확인을 함께 적용하고, 두 응답의 완료 순서를 뒤집은 뒤 Query 캐시와 저장값이 모두 마지막 단계를 유지하는지 검증했다.

## 결정 5. 상세 패널은 외부 클릭과 닫기 버튼으로 닫는다

### 배경

상세 패널의 닫기 동작을 명확히 정해야 한다.

### 최종 결정

카드의 정보 영역을 상세 열기 버튼으로 사용한다. Radix Dialog Primitive로 오른쪽 상세 패널을 만들고 오버레이 또는 닫기 버튼을 클릭하면 닫는다. 패널 내부 클릭은 유지하고 ESC 키 닫기는 구현하지 않는다.

### 이유와 트레이드오프

요구한 상호작용을 그대로 적용한다. 카드 하단 단계 이동 버튼과 상세 열기 이벤트가 충돌하지 않도록 영역을 나눈다. Radix Dialog가 모달 외부 상호작용 차단과 포커스 범위를 관리하고, 열릴 때 닫기 버튼으로 포커스를 이동한 뒤 닫힐 때 원래 카드로 복귀시킨다. 기본 ESC 닫기를 막았기 때문에 키보드 사용자는 포커스 가능한 닫기 버튼을 사용한다.

### AI 제안 검토

AI 초안은 추가 의존성 없이 React Portal, 직접 작성한 오버레이 이벤트와 포커스 복귀 effect를 사용했다. 이 방식은 간단하지만 포커스 가두기와 배경 요소 비활성화를 직접 구현해야 하므로 기각했다. shadcn/ui 전체 컴포넌트 설치 대신 실제 필요한 `@radix-ui/react-dialog`만 추가하고 ESC 동작만 요구사항에 맞게 재정의했다.

## 요구사항에 대한 가정

- 검색어와 직무 필터는 동시에 적용한다.
- 전체 지원자가 없는 상태와 현재 검색 결과가 없는 상태를 구분한다.
- 최종합격과 불합격을 포함한 모든 단계로 명시적 이동이 가능하다고 가정한다.
- 약 15%의 실패는 API 계층에서 발생시키며, 평가용 강제 실패 방법을 별도로 제공한다.

## 완료하지 못한 기능

최종 제출 시 완료하지 못한 기능이 있을 경우 기능명, 이유, 대신 우선한 작업과 후속 구현 방법을 기록한다. 완료하지 못한 기능이 없으면 없다고 명시한다.
