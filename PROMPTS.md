# AI 협업 기록

기능 구현 순서와 커밋 순서에 맞춰 실제 프롬프트, AI 출력 요지, 리뷰 및 검증 결과를 기록합니다. 기능별 최초 요청, 요구사항 변경, 기술 선택, 구현 방향 변경과 중요한 검증·수정에 관한 주요 프롬프트는 모두 남기고, 오타·띄어쓰기·표현 정리 같은 사소한 수정은 제외합니다.

## 기록 기준

- 제품 기능은 과제의 필수 요구사항과 GitHub 이슈에서 정한 범위를 기준으로 나누고, 기능 커밋 하나와 아래 기록 한 섹션이 대응되게 정리했습니다.
- 인용문은 실제 사용자 요청을 우선 기록하고, 작업 범위를 구체화한 이슈 본문과 병렬 작업에 사용한 서브에이전트 지시는 출처를 구분했습니다.
- 기능 구현과 무관한 승인, 단순 PR 생성 요청, 오타 수정은 제외했습니다. 요구사항이나 구현 방향을 바꾼 피드백은 모두 남겼습니다.
- 저장소 운영, CI와 배포 과정에서 이어진 보완 커밋은 제품 기능과 구분해 같은 작업 흐름 안에 시간순으로 기록했습니다.

최종 문서 정리와 제출 검증은 [#12 docs(submission): 제출 문서와 검증 결과를 정리한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/12)에서 추적합니다.

## 필수 요구사항 대응

| 필수 요구사항 | 기능 식별자 | 관련 커밋과 기록 |
| --- | --- | --- |
| 보드, 컬럼, 카드와 필수 정보 | `board-layout`, `card-list` | `feat(board-layout)`, `feat(card-list)` / 08, 09 |
| 명시적 액션 단계 이동, API 저장과 새로고침 유지 | `mock`, `stage` | `feat(mock)`, `feat(stage)` / 06, 10 |
| 낙관적 업데이트, 실패 rollback과 피드백 | `optimistic-update` | `feat(optimistic-update)` / 11 |
| 이름 검색, 직무 필터와 200건 처리 | `filter` | `feat(filter)` / 13 |
| 카드에서 지원자 상세 보기 | `detail` | `feat(detail)` / 14 |
| 로딩, 오류와 빈 상태 | `state` | `feat(state)` / 15 |
| Mock API 지연 200~800ms와 약 15% 실패 | `mock` | `feat(mock)` / 06 |

도전 요구사항 중 같은 카드의 연속 이동 경쟁 상태는 `race-condition` 기능과 `fix(race-condition)` 커밋으로 분리해 12번에 기록했습니다. 핵심 로직 테스트와 키보드 접근성 검증은 각 기능의 리뷰 / 검증에 함께 남겼습니다.

## 01. [harness] 과제 요구사항과 수행 하네스 구성

### 관련 커밋

`chore(harness): 과제 수행 하네스를 구성한다`

### 프롬프트 1 (사용자 요청)

> 원문을 바탕으로 하네스를 만들려고 하는데 스킬이 좋을까?

### 프롬프트 2 (사용자 요청)

> 이 과제 하나에만 쓸 거야. 저장소 생성해줘. AGENTS.md랑 sh도 한글로 작성.

### AI 출력 요지

- 여러 프로젝트에서 재사용하지 않는다면 전역 스킬보다 저장소 내부의 `AGENTS.md`와 검증 스크립트가 적합하다고 제안했다.
- 기능 하나, 커밋 하나, `PROMPTS.md` 섹션 하나가 대응되도록 작업 절차를 구성했다.
- 필수 문서와 커밋 형식, MSW 설정, 자동 검증 명령을 확인하는 스크립트를 제안했다.

### 리뷰 / 검증

- 과제 원문을 다시 읽어 필수 제출 문서와 커밋 규칙을 확인했다.
- 다른 프로젝트의 변경사항이 있는 현재 작업 폴더를 그대로 사용하지 않고 독립 저장소로 분리했다.
- 애플리케이션 구현 전이므로 README에는 완료되지 않은 기능을 구현 완료로 표현하지 않았다.
- 검증 스크립트 첫 실행에서 Bash가 한글 변수명을 식별하지 못하는 문제를 재현했다.
- 사용자에게 표시되는 문구는 한글로 유지하고, 실행에 필요한 내부 변수와 함수명만 ASCII 식별자로 수정했다.
- 수정 후 구문 검사와 전체 검증 스크립트를 다시 실행해 동작을 확인했다.

## 02. [workflow] Pull Request 기반 작업 흐름으로 변경

### 관련 커밋

`docs(workflow): Pull Request 기반 작업 절차를 추가한다`

### 프롬프트 1 (사용자 요청)

> 바로 커밋하지 말고 PR 생성하는 식으로 바꾸고 싶어.

### AI 출력 요지

- `main`에 직접 커밋하지 않고 기능 브랜치에서 작업한 뒤 PR로 병합하는 절차를 제안했다.
- 과제에서 커밋 히스토리 보존을 요구하므로 squash나 rebase 대신 merge commit 방식을 제안했다.
- 기능 하나를 브랜치 하나와 PR 하나로 대응하도록 작업 규칙을 변경했다.

### 리뷰 / 검증

- PR은 커밋을 기반으로 생성되므로 "커밋하지 않고 PR 생성"은 불가능하다는 점을 확인했다.
- 요구 의도를 "main에 바로 커밋하지 않는다"로 해석해 브랜치, 기능 커밋, PR, merge commit 순서로 조정했다.
- 최초 저장소 생성 커밋은 원격 저장소와 브랜치가 없던 초기화 단계이므로 예외로 명시했다.
- `main`에서 미커밋 변경이 발생하면 검증 스크립트가 실패하도록 추가했다.

## 03. [ci] Pull Request 자동 검증 구성

### 관련 커밋

- `chore(ci): Pull Request 자동 검증을 추가한다`
- `fix(ci): CI 커밋 기록 검증을 바로잡는다`
- `docs(prompts): CI 원격 검증 결과를 기록한다`

### 프롬프트 1 (사용자 요청)

> 해당프로젝트 GitHub Actions를 추가해 PR마다 validate-submission.sh, 테스트, 린트, 빌드를 실행하도록 구성해줘

### AI 출력 요지

- `main` 대상 Pull Request마다 제출 검증 스크립트, 테스트, 린트, 빌드를 실행하는 GitHub Actions 워크플로를 제안했다.
- 최소 권한과 동시 실행 취소를 설정하고, 재현 가능한 의존성 설치를 위해 `npm ci`를 사용하도록 제안했다.
- 아직 애플리케이션이 초기화되지 않은 현재 상태에서는 Node 검증을 보류하고, 초기화 후 lockfile이나 필수 스크립트가 누락되면 실패하도록 제안했다.

### 리뷰 / 검증

- 현재 저장소에 `package.json`이 없음을 확인해 애플리케이션 코드나 임시 npm 스크립트를 이번 CI 변경에 섞지 않았다.
- `package.json`이 추가된 이후에는 `package-lock.json`, `test:run`, `lint`, `build`가 모두 있어야 Node 검증을 진행하도록 경계 조건을 추가했다.
- merge commit을 요구하면서 모든 커밋 제목에 기능 커밋 형식을 적용하던 제출 스크립트의 모순을 발견해 merge commit은 제목 검사에서 제외했다.
- GitHub 공식 문서와 공식 Actions 릴리스를 확인해 Pull Request 트리거, 읽기 전용 권한, Node 설정 방식을 검토했다.
- 워크플로 YAML 구문, Node 프로젝트 확인 스크립트의 보류·성공·실패 분기, 전체 제출 검증 스크립트를 직접 실행해 확인했다.
- PR의 첫 GitHub Actions 실행은 통과했지만 로그에서 shallow checkout과 detached HEAD 때문에 커밋 수와 브랜치명이 부정확한 문제를 발견했다.
- 전체 Git 이력을 checkout하고 `GITHUB_HEAD_REF`를 우선 사용하도록 수정한 뒤 두 번째 원격 실행에서 커밋 7개, 커밋 제목 형식, `chore/github-actions` 브랜치 인식을 확인했다.
- 두 번째 GitHub Actions 실행은 제출 검증 19개 통과, 경고 2개, 실패 0개로 완료됐고 애플리케이션 초기화 전 Node 검증 보류 안내도 확인했다.

## 04. [parallel-workflow] 병렬 서브에이전트 작업 정책

### 관련 커밋

`docs(workflow): 병렬 서브에이전트 작업 규칙을 추가한다`

### 프롬프트 1 (사용자 문제 제기)

> 너가 말한 문서는 목차 역할이고 서브에이전트로 기능들을 돌리면 충돌이 날 수 있을 것 같아.

### 프롬프트 2 (사용자 요구사항)

> 한 번에 한 이슈, PR 병합 후 다음 작업 시작 부분을 병렬 작업할 수 있도록 수정해줘.

### 프롬프트 3 (사용자 요구사항)

> 혹시 병렬 작업 막아져 있어? 해당 부분도 허용해줘.

### AI 출력 요지

- 기능 구현을 완전히 직렬화하면 충돌은 줄지만 독립 기능까지 기다려야 하는 문제가 있다고 분석했다.
- 독립 worktree, 기능 브랜치, 파일 소유권을 사용한 통제된 병렬 개발을 제안했다.
- 공통 문서와 의존성 파일은 메인 에이전트가 단독으로 통합하고 사용자가 PR을 직접 병합하는 방식을 제안했다.

### 리뷰 / 검증

- 제한 없는 병렬 작업은 `PROMPTS.md`, lock 파일, 공통 타입과 애플리케이션 진입점에서 충돌할 가능성이 높아 기각했다.
- 의존성이 없고 수정 파일이 겹치지 않는 이슈만 병렬로 진행하도록 범위를 제한했다.
- 서브에이전트는 독립 worktree에서 소유 파일만 수정하고 메인 에이전트가 문서, 테스트, 커밋과 PR을 통합하도록 조정했다.
- 공유 파일을 수정하거나 선행 관계가 있는 작업은 stacked PR 또는 순차 통합을 사용하도록 정했다.
- 자동 승인과 자동 병합은 사용하지 않고 저장소 소유자가 CI와 변경사항을 확인한 뒤 merge commit으로 병합하도록 유지했다.

## 05. [app] 애플리케이션 기술 스택과 개발 환경 구성

### 관련 이슈

[#4 chore(app): 기술 스택과 개발 환경을 구성한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/4)

### 관련 커밋

`chore(app): 기술 스택과 개발 환경을 구성한다`

### 프롬프트 1 (사용자 기술 비교 요청)

> #4 이슈에서 Next.js랑 Vite React 중에 고민인데 요구사항을 구현하려면 장단점 비교해줘.

### 프롬프트 2 (사용자 기술 비교 요청)

> 스타일링도 shadcn이랑 Tailwind CSS 중에서 고민인데 장단점 비교해줘.

### 프롬프트 3 (사용자 상태 관리 비교 요청)

> 외부 상태 관리를 사용할지도 논의가 필요한데 Zustand를 쓸지 말지와 내부 상태 관리의 장단점을 비교해줘.

### 프롬프트 4 (사용자 구현 요청)

> #4 이슈 진행해줘. 우리가 나눈 스택 관련 결정 부분은 DECISIONS.md에 명시해줘.

### AI 출력 요지

- 단일 클라이언트 보드와 MSW, `localStorage` 중심 요구사항에는 Next.js보다 Vite + React가 단순하다고 제안했다.
- shadcn/ui는 Tailwind CSS의 대안이 아니라 Tailwind 기반 컴포넌트 코드 배포 방식이므로, Tailwind를 기본으로 사용하고 복잡한 UI Primitive만 선택적으로 추가하자고 제안했다.
- 후보자 API 상태는 TanStack Query, 검색·필터·상세 선택은 React 내부 상태로 관리하고 Zustand에는 후보자 데이터를 중복 저장하지 말자고 제안했다.
- 최신 Vite와 Node 호환성, MSW browser worker, 테스트와 품질 검사 환경을 함께 구성했다.

### 리뷰 / 검증

- 과제 요구사항에 서버 렌더링, SEO와 다중 라우트가 없고 상호작용 대부분이 브라우저에서 실행되는 것을 확인해 Vite 제안을 채택했다.
- shadcn/ui 전체 설치는 아직 사용하지 않는 코드가 생기므로 기각하고, 실제 기능에서 필요한 컴포넌트만 추가하기로 범위를 줄였다.
- TanStack Query 캐시와 Zustand Store에 후보자 데이터를 함께 저장하면 rollback 시 동기화 대상이 늘어나는 문제를 확인해 Zustand 도입을 기각했다.
- 최신 Vite가 Node 20.19 또는 22.12 이상을 요구하는 것을 패키지 메타데이터로 확인했다. 지원이 종료된 Node 20 대신 로컬에 설치된 Node 22를 기준으로 `package.json`과 README에 Node 22.12 이상을 명시했다.
- ESLint, 타입 검사, Vitest 테스트 2개와 프로덕션 빌드를 Node 22.22.2에서 실행해 모두 통과한 것을 확인했다.
- 저장소 검증 스크립트는 통과 24개, 경고 2개, 실패 0개였다. 약 15% 실패 handler 경고는 이슈 #5 범위이고, 작업 트리 경고는 커밋 전 검증이므로 유지했다.
- 개발 서버에서 MSW Worker 시작 후 화면이 렌더링되는 것을 확인했다. 데스크톱과 375px 모바일 화면을 검사했고 모바일 가로 오버플로가 없었다.
- PR 첫 원격 검증에서 `npm ci`보다 제출 하네스가 먼저 실행되어 ESLint, Vite와 Vitest를 찾지 못하는 문제를 발견했다. 로컬에는 이미 `node_modules`가 있어 드러나지 않았던 CI 순서 문제였다.
- GitHub Actions를 Node 설정, 프로젝트 확인, 의존성 설치, 통합 제출 검증 순서로 수정했다. 하네스가 테스트·린트·타입 검사·빌드를 모두 실행하므로 중복 단계는 제거하고 필수 스크립트 확인에 `typecheck`를 추가했다.

## 06. [mock] 후보자 Mock API와 저장 계층

### 관련 이슈

[#5 feat(mock): 후보자 Mock API와 저장 계층을 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/5)

### 관련 커밋

`feat(mock): 후보자 Mock API와 저장 계층을 구현한다`

### 프롬프트 1 (사용자 요구사항)

> Mock API는 MSW로 하자. Mock API에 대한 네트워크 지연(200~800ms) 시뮬레이션 부분과 약 15% 실패가 빠졌어.

### 프롬프트 2 (사용자 구현 요청)

> #5 이슈도 진행하자.

### 프롬프트 3 (이슈 #5 구현 조건)

> 후보자 타입과 200건 이상의 seed 데이터를 만들고, `GET /api/candidates`와 `PATCH /api/candidates/:candidateId/stage`를 MSW로 구현해줘. 모든 요청에 200~800ms 무작위 지연과 약 15% 실패를 적용하고, 변경 단계는 localStorage에 저장해 새로고침 후에도 유지해줘. 테스트에서는 성공과 실패를 결정적으로 재현할 수 있어야 해.

### AI 출력 요지

- 후보자 타입, 200건 seed, localStorage 저장소, 네트워크 정책과 MSW handler를 역할별 파일로 분리했다.
- `GET /api/candidates`와 `PATCH /api/candidates/:candidateId/stage`를 구현하고 모든 요청에 200~800ms 지연과 15% 실패를 적용했다.
- 무작위 실패 때문에 테스트가 불안정해지지 않도록 `always`, `never`, `random` 실패 모드를 제안했다.
- 저장 데이터 손상, 유효하지 않은 단계와 존재하지 않는 후보자 오류를 구분했다.

### 리뷰 / 검증

- 무작위 지연과 실패를 handler 내부의 고정된 값으로 구현하면 경계값을 검증하기 어려워 지연 계산과 실패 판정을 순수 함수로 분리했다.
- 테스트에서 지연 자체를 제거하는 방식은 모든 요청에 200~800ms 지연을 적용한다는 요구사항을 우회하므로 기각했다. 성공과 실패만 결정적으로 제어하고 실제 지연은 통합 테스트에서도 유지했다.
- 후보자 배열을 모듈 변수에만 저장하면 새로고침 후 변경이 사라지므로 localStorage를 단일 저장 원본으로 사용했다.
- 손상된 JSON이나 잘못된 후보자 구조는 그대로 반환하지 않고 200건 seed로 복구하도록 보완했다.
- 린트와 타입 검사가 통과했고, seed·저장소·네트워크 정책·GET·PATCH·강제 실패·400·404를 포함한 테스트 17개가 통과했다.
- 제출 하네스는 통과 25개, 경고 1개, 실패 0개였다. 남은 경고는 기능 커밋 전 작업 트리 변경사항이었다.

## 07. [deploy] GitHub Actions 검증과 Vercel 배포 연결

### 관련 커밋

`chore(deploy): 검증 후 Vercel 배포를 연결한다`

`fix(lint): Vercel 산출물을 검사에서 제외한다`

### 프롬프트 1 (사용자 CI 요청)

> 해당프로젝트 GitHub Actions를 추가해 PR마다 validate-submission.sh, 테스트, 린트, 빌드를 실행하도록 구성해줘

### 프롬프트 2 (사용자 승인 정책)

> pr승인은 내가할수있도록해줘

### 프롬프트 3 (사용자 배포 요청)

> vercel 연동해줘

### 프롬프트 4 (사용자 배포 흐름 요청)

> githubaction 이랑 vercel배포랑 연동시키고싶어

### AI 출력 요지

- `main` 대상 Pull Request와 `main` push에서 제출 검증을 먼저 실행하고, 성공한 경우에만 Vercel을 배포하도록 제안했다.
- Pull Request는 Preview, `main` push는 Production 환경으로 분리하고 포크 Pull Request에는 GitHub Actions Secrets를 제공하지 않도록 제안했다.
- Vercel CLI의 `pull`, `build`, `deploy --prebuilt` 순서로 같은 Actions 실행 안에서 빌드와 배포를 수행하도록 구성했다.
- PR 승인과 병합은 자동화하지 않고 저장소 소유자가 직접 수행하도록 유지했다.

### 리뷰 / 검증

- Vercel Git 연동만 사용하면 GitHub Actions 검증 결과와 독립적으로 배포될 수 있어 기각하고, `validate` 작업을 배포 작업의 선행 조건으로 지정했다.
- GitHub Actions와 Vercel Git 연동을 함께 유지하면 같은 커밋이 두 번 배포되는 것을 확인해 Vercel 자체 Git 연결을 해제했다. Vercel 프로젝트와 기존 배포는 유지하고 Actions만 배포 주체로 사용한다.
- `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`를 저장소 Actions Secrets에 등록했으며 값은 로그와 문서에 기록하지 않았다.
- Node 22.22.2에서 의존성 설치, 린트, 타입 검사, 테스트와 프로덕션 빌드를 실행해 통과했고 Vercel Preview 환경의 prebuilt 빌드도 통과했다.
- 로컬 Vercel 빌드 후 제출 검증을 반복하자 ESLint가 `.vercel/output`의 생성 번들을 검사하는 문제를 발견했다. 생성물을 수정하지 않고 ESLint 전역 제외 경로에 `.vercel`을 추가했다.
- PR #19에서 제출 검증과 GitHub Actions 기반 Vercel Preview 배포가 통과한 것을 확인했다. Production 배포는 사용자가 PR을 병합한 뒤 `main` push에서 실행되므로 아직 완료로 기록하지 않는다.

## 08. [board-layout] 단계별 파이프라인 보드 레이아웃

### 관련 이슈

[#6 feat(board): 파이프라인 컬럼과 지원자 카드를 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/6)

### 관련 커밋

`feat(board-layout): 단계별 파이프라인 컬럼을 구현한다`

### 프롬프트 1 (사용자 구현 요청)

> #6 이슈 진행.

### 프롬프트 2 (이슈 #6 구현 조건)

> 서류검토, 면접, 처우협의, 최종합격, 불합격 컬럼을 항상 같은 순서로 표시하고 보드와 카드의 기본 시맨틱 마크업을 구성한다.

### AI 출력 요지

- 채용 단계 정의를 타입에서 파생해 라벨과 렌더링 순서가 분리되지 않게 구성했다.
- 각 컬럼을 제목이 연결된 `section`으로 만들고 지원자 수를 별도 접근성 이름으로 제공했다.
- 좁은 화면에서 컬럼을 세로로 재배치하지 않고 가로 스크롤로 단계 비교 흐름을 유지했다.

### 리뷰 / 검증

- 화면 크기에 따라 컬럼을 줄바꿈하면 단계 순서와 카드 위치 비교가 어려워질 수 있어 기각했다. 다섯 컬럼의 안정적인 너비와 가로 스크롤을 선택했다.
- 단계 라벨 배열을 별도로 다시 작성하면 타입의 순서와 달라질 수 있어 기존 `PIPELINE_STAGES`와 라벨 맵에서 정의를 생성하도록 수정했다.
- 컬럼 제목을 시각적 텍스트로만 두지 않고 보드와 컬럼에 `aria-labelledby`를 연결했다.
- 첫 테스트에서 이전 테스트의 DOM이 남아 같은 접근성 이름이 중복되는 문제를 재현했다. Vitest 전역 API를 사용하지 않는 설정에서는 자동 cleanup이 등록되지 않아 공통 테스트 설정에 명시적 cleanup을 추가했다.
- 테스트에서 다섯 단계의 개수와 표시 순서, 단계별 지원자 수 접근성 이름을 확인했다.

## 09. [card-list] 지원자 카드와 조회 데이터 연결

### 관련 이슈

[#6 feat(board): 파이프라인 컬럼과 지원자 카드를 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/6)

### 관련 커밋

`feat(card-list): 지원자 카드와 데이터 연결을 구현한다`

### 프롬프트 1 (사용자 구현 요청)

> #6 이슈 진행.

### 프롬프트 2 (이슈 #6 구현 조건)

> TanStack Query로 후보자 목록을 조회하고, 하나의 후보자 배열을 현재 단계에 따라 그룹화해 카드로 표시한다. 카드에는 이름, 직무, 지원일, 현재 단계를 노출한다.

### AI 출력 요지

- TanStack Query 조회 훅과 순수한 보드 표시 컴포넌트를 분리해 서버 상태와 렌더링 책임을 나눴다.
- 후보자 배열을 단계별 Record로 한 번 그룹화하고 각 컬럼이 해당 배열만 렌더링하도록 구성했다.
- 지원자 카드를 이름으로 식별되는 `article`로 만들고 직무, 지원일, 현재 단계에 의미 있는 마크업을 적용했다.

### 리뷰 / 검증

- 컬럼별 후보자 배열을 별도 상태로 저장하면 단계 변경 시 중복 상태가 생기므로 단일 API 배열을 렌더링 시점에 그룹화하도록 결정했다.
- 현재 단계 텍스트가 시각적 배지와 접근성 전용 요소에 중복되지 않도록 배지 자체를 설명 목록의 값으로 연결했다.
- 전체 로딩·오류 UX는 후속 이슈 범위지만 비동기 조회 중 빈 화면이 되지 않도록 최소 상태 문구를 제공했다.
- 카드의 네 필드, 단계별 개수, 지원자 1회 표시와 MSW 응답의 TanStack Query 연결을 테스트했다.
- 실제 브라우저에서 카드 200개, 다섯 단계의 고정 순서와 페이지 전체 넘침 없이 동작하는 보드 내부 가로 스크롤을 확인했다.

## 10. [stage] 명시적 액션을 사용한 지원자 단계 이동

### 관련 이슈

[#7 feat(stage): 지원자 단계 이동과 API 저장을 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/7)

후속 범위: [#20 feat(order): 컬럼 내 지원자 카드 순서 변경을 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/20)

### 관련 커밋

`feat(stage): 지원자 단계 이동과 API 저장을 구현한다`

### 프롬프트 1 (사용자 의사결정 요청)

> #7 이슈 진행하려하는데 나한테 의사결정할거리 질문해줘.

### 프롬프트 2 (사용자 구현 요청)

> 진행하자. 추천하는 방식으로 가고 로컬 띄워줘.

### 프롬프트 3 (사용자 UX 검토)

> 명시적 액션 버튼이 빠진 것 같은데.

### 프롬프트 4 (사용자 방향 변경)

> 아까 방식으로 변경하자. 카드에 `<`, `>` 표시를 해서 다음 단계로도 이동할 수 있게 해줘.

### 프롬프트 5 (사용자 배치 수정)

> 위치 왜 그래? 카드 하단으로 옮겨줘.

### 프롬프트 6 (사용자 범위 검토)

> 칸반이면 위아래 이동도 가능해야 할 것 같아.

> 아냐, UX가 불편해. 이슈 업데이트하고 그것도 반영해줘. 대신 md 문서에 해당 논의 내역은 남겨줘.

> 관련 부분에 대해 따로 이슈 추가해줘.

### 프롬프트 7 (사용자 최종 결정)

> 명시적 액션 버튼으로 한 이유를 명시해줘. 예전에 드래그앤드롭을 구현했을 때 이슈가 있었고 제한 시간 내로 하려면 버그가 없어야 해. 대신 selectbox와 좌우 버튼을 통해 단계 이동을 자유롭게 했어. 위아래는 필수 요구사항이 아니라서 #20 이슈만 생성하고 넘어갔어.

### AI 출력 요지

- 현재 단계를 표시하는 드롭다운 메뉴에서 모든 채용 단계를 선택하고 PATCH 요청으로 저장하는 구조를 제안했다.
- 요청 중에는 해당 카드의 액션을 비활성화하고 성공 응답 이후에만 TanStack Query 캐시를 갱신하며, 실패하면 기존 컬럼과 카드별 오류를 유지하도록 제안했다.
- 키보드 포커스와 메뉴 탐색을 안정적으로 제공하기 위해 필요한 Radix Dropdown Menu Primitive만 선택적으로 도입했다.

### 리뷰 / 검증

- DnD는 이전 구현에서 잘못된 위치 이동과 상태 불일치를 경험했고 제한 시간 안에 키보드와 실패 흐름까지 안정적으로 검증하기 어렵다는 사용자 판단에 따라 기각했다.
- AI 초안의 단계 메뉴만으로는 인접 단계 반복 이동이 불편하다는 피드백을 반영해 카드 하단에 왼쪽·오른쪽 버튼을 추가했다. 가운데 단계 메뉴는 임의 단계 이동 경로로 유지했다.
- 같은 컬럼 안의 위·아래 카드 순서 변경은 별도 저장 API가 필요하고 필수 요구사항이 아니므로 구현에 섞지 않고 [#20](https://github.com/LeeEugene1/hiring-pipeline-board/issues/20)으로 분리했다.
- #7에서는 낙관적 업데이트를 제외하고 PATCH 성공 후에만 카드를 이동해 실패 시 rollback 없이 서버와 화면 상태를 일치시켰다.
- 통합 테스트에서 현재 단계 비활성화, 키보드 메뉴 선택, 좌우 버튼 경계, PATCH 요청 값, 성공 후 컬럼 이동, 재조회 후 단계 유지와 503 실패 피드백을 검증했다.
- 테스트 25개와 린트가 통과했다. 실제 브라우저에서는 단계 메뉴 이동과 새로고침 이후 저장 상태 유지를 확인했다.

## 11. [optimistic-update] 낙관적 단계 이동과 실패 롤백

### 관련 이슈

[#8 feat(stage): 낙관적 업데이트와 실패 복구를 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/8)

### 관련 커밋

`feat(optimistic-update): 낙관적 이동과 실패 롤백을 구현한다`

### 프롬프트 1 (사용자 구현 요청)

> #8 이슈 진행해줘.

### 프롬프트 2 (사용자 오류 흐름 검토)

> `Mock API 요청에 실패했습니다.` 실패하면 낙관적 업데이트를 하는 게 아니라 rollback해야 하는 거 아니야? 너가 말한 실패 흐름이 일반적이야?

### 프롬프트 3 (사용자 수정 요청)

> 오케이, 해당 부분 수정하자.

### AI 출력 요지

- mutation 직전에 후보자 snapshot을 저장하고 Query 캐시의 단계를 즉시 바꾸는 낙관적 업데이트를 제안했다.
- PATCH 실패 시 snapshot을 복원하고 오류 메시지를 표시하는 일반적인 rollback 흐름을 설명했다.
- 진행 중인 후보자 조회를 취소해 오래된 GET 응답이 낙관적 상태를 덮지 않도록 제안했다.

### 리뷰 / 검증

- 오류 메시지만 표시하고 변경된 컬럼에 카드를 남기는 동작은 rollback이 아니므로 기각했다. 응답 전 즉시 이동, 실패 후 원래 컬럼 복구, 오류 표시 순서를 명시했다.
- 전체 후보자 배열 snapshot을 복원하면 동시에 변경된 다른 후보자의 상태를 덮을 수 있어 해당 후보자 snapshot만 저장하고 복구하도록 수정했다.
- 낙관적 이동 후 카드가 다른 컬럼에서 다시 마운트되어 로컬 mutation 오류가 사라질 수 있으므로 TanStack Mutation Cache에서 후보자별 최신 상태를 조회하도록 보완했다.
- 테스트에서 PATCH 응답을 보류해 응답 전 이동을 먼저 확인하고, 503 반환 후 원래 단계와 이름이 복구되는지 검증했다. 대기 중 다른 후보자의 캐시를 변경해 rollback 이후에도 해당 변경이 유지되는 것도 확인했다.
- 테스트 25개, 린트와 타입 검사가 통과했다. 커밋 전 로컬 서버에서 실패와 복구 흐름을 확인할 수 있게 유지한 뒤 첫 기능 커밋으로 기록했다.

## 12. [race-condition] 연속 단계 이동 경쟁 상태 처리

### 관련 이슈

[#8 feat(stage): 낙관적 업데이트와 실패 복구를 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/8)

### 관련 커밋

`fix(race-condition): 연속 이동 시 요청 경쟁 상태를 처리한다`

### 프롬프트 1 (사용자 구현 요청)

> #8 이슈 진행해줘.

### 프롬프트 2 (이슈 #8 구현 조건)

> 추가로 동일 카드 연속 이동 시 최신 요청이 우선되게 하고, 응답 순서가 역전되어도 마지막 사용자 선택을 유지해줘.

### AI 출력 요지

- 후보자별 요청 번호를 발급하고 최신 요청과 일치하는 callback만 캐시를 확정하거나 rollback하는 방식을 제안했다.
- 새 단계 선택 시 이전 요청의 `AbortController`를 중단해 불필요한 이전 응답 처리를 막도록 제안했다.
- Mock API도 저장 직전에 중단 신호를 확인해 오래된 요청이 localStorage를 덮지 않도록 제안했다.

### 리뷰 / 검증

- 오래된 응답을 UI에서만 무시하면 Mock API 저장값은 이전 단계가 될 수 있어 불충분하다고 판단했다. 클라이언트 callback과 Mock API 저장 양쪽에 최신 요청 우선 처리를 적용했다.
- 연속 요청 중 최신 요청이 실패하면 중단된 이전 낙관 상태가 아니라 마지막 서버 확정 snapshot으로 복구하도록 후보자별 확정 상태를 유지했다.
- 진행 중에도 다음 단계를 선택할 수 있도록 액션을 비활성화하지 않고 현재 단계 옆에 로딩 아이콘만 표시했다.
- 테스트에서 첫 요청과 두 번째 요청의 응답을 각각 보류하고 두 번째 응답을 먼저 완료했다. 첫 요청을 나중에 종료한 뒤에도 Query 캐시와 Mock API 저장값이 마지막 선택인 처우협의를 유지하는지 확인했다.
- 경쟁 상태 테스트를 포함해 테스트 26개, 린트와 타입 검사가 통과했다.

## 13. [filter] 이름 검색과 직무 필터

### 관련 이슈

[#9 feat(filter): 이름 검색과 직무 필터를 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/9)

### 관련 커밋

`feat(filter): 이름 검색과 직무 필터를 구현한다`

### 프롬프트 1 (사용자 요청)

> 이슈 #9, #10, #11 병렬로 실행해서 진행하고 싶어.

### 프롬프트 2 (이슈 #9 구현 조건)

> 이름 부분 검색과 직무 선택 필터를 함께 제공하고, 대소문자와 앞뒤 공백을 정규화해 두 조건의 교집합을 표시해줘. 200건 이상에서도 부드럽게 동작하고 필터 초기화와 결과 없음 상태를 전달해야 해.

### AI 출력 요지

- 검색·필터 계산을 UI에서 분리한 순수 함수와 검색 컨트롤 컴포넌트를 제안했다.
- 이름 검색어에는 `useDeferredValue`, 직무 목록과 필터 결과에는 `useMemo`를 적용해 200건 렌더링 중 입력 반응성을 유지하도록 제안했다.
- 전체 직무 옵션, 결과 수와 검색·필터 동시 초기화 버튼을 제공했다.

### 리뷰 / 검증

- 검색과 select 변경을 모두 지연시키는 대신 타이핑 비용이 있는 이름 검색만 deferred value로 처리하고 직무 변경은 즉시 반영하도록 수정했다.
- 정규화 로직을 컴포넌트 안에 두면 경계 조건과 200건 처리를 분리해 검증하기 어려워 순수 함수로 이동했다.
- 결과 수는 시각적으로 `결과 / 전체`를 표시하고 변경 내용을 보조 기술에 전달할 수 있도록 접근성 이름과 `aria-live`를 추가했다.
- 이름의 대소문자·공백 정규화, 부분 일치, 직무 교집합, 직무 중복 제거, 200건 처리와 화면 초기화를 포함한 전용 테스트 10개가 통과했다. 린트와 타입 검사도 통과했다.

## 14. [detail] 지원자 상세 패널

### 관련 이슈

[#10 feat(detail): 지원자 상세 패널을 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/10)

### 관련 커밋

`feat(detail): 지원자 상세 패널을 구현한다`

### 프롬프트 1 (사용자 요청)

> 이슈 #9, #10, #11 병렬로 실행해서 진행하고 싶어.

### 프롬프트 2 (사용자 닫기 정책)

> ESC 키는 빼고 상세 패널 외부 클릭 시 상세 패널을 닫아줘.

### 프롬프트 3 (서브에이전트 구현 지시)

> GitHub 이슈 #10 지원자 상세 패널의 전용 모듈만 구현하세요. 요구사항: 카드 클릭과 키보드로 열 수 있는 상세 패널을 위한 컴포넌트 API, 오버레이 클릭과 닫기 버튼으로 닫힘, 패널 내부 클릭 유지, ESC는 닫히지 않음, 열릴 때 패널 내부로 포커스 이동, 닫힐 때 트리거로 포커스 복귀, dialog 접근성 이름 제공. 커밋과 푸시는 하지 마세요.

### AI 출력 요지

- 직접 만든 React Portal과 제어형 `candidate`, `onClose`, `triggerRef` API로 상세 패널을 구성하는 초안을 만들었다.
- 카드의 정보 영역과 단계 이동 영역을 분리하고 정보 영역을 버튼으로 만들어 마우스와 키보드에 같은 실행 경로를 제공했다.
- 연락처, 지원 정보, 경력과 요약을 제목이 연결된 dialog에 표시했다.

### 리뷰 / 검증

- 직접 만든 Portal은 포커스 가두기, 배경 상호작용 차단과 모달 접근성을 별도로 유지해야 하므로 최종안으로 채택하지 않았다. 기존 기술 결정에 맞춰 필요한 Radix Dialog Primitive만 추가했다.
- Radix Dialog의 기본 ESC 닫기는 사용자 요구와 다르므로 `onEscapeKeyDown`에서 명시적으로 막고, 오버레이와 닫기 버튼은 기본 dismiss 동작을 사용했다.
- 카드 전체를 중첩 버튼으로 만들면 하단 단계 이동 액션과 충돌하므로 정보 영역만 상세 열기 버튼으로 만들고 단계 액션을 별도 영역에 유지했다.
- 접근성 이름과 상세 필드, 내부·외부 클릭, ESC 예외, 열기·닫기 포커스, 카드에서 패널까지의 통합을 포함한 전용 테스트 9개가 통과했다. 린트와 타입 검사도 통과했다.

## 15. [state] 로딩, 오류와 빈 상태

### 관련 이슈

[#11 feat(state): 로딩 오류 빈 상태를 구현한다](https://github.com/LeeEugene1/hiring-pipeline-board/issues/11)

### 관련 커밋

`feat(state): 로딩 오류 빈 상태를 구현한다`

### 프롬프트 1 (사용자 요청)

> 이슈 #9, #10, #11 병렬로 실행해서 진행하고 싶어.

### 프롬프트 2 (서브에이전트 구현 지시)

> GitHub 이슈 #11의 보드 상태 UI 전용 모듈만 구현하세요. 요구사항: 초기 로딩(보드 형태 스켈레톤), GET 오류와 재시도 버튼, 전체 지원자 없음, 검색·필터 결과 없음 및 필터 초기화 액션을 서로 구분하고 접근성 상태를 갖추세요. 커밋과 푸시는 하지 마세요.

### AI 출력 요지

- 초기 로딩, 조회 오류, 전체 목록 없음과 필터 결과 없음을 독립된 상태 컴포넌트로 분리했다.
- 필터 빈 상태에서만 초기화 callback을 필수로 받는 판별 유니언 props를 제안했다.
- 오류 재시도 중에는 버튼을 비활성화하고 로딩 아이콘과 문구를 변경하도록 제안했다.

### 리뷰 / 검증

- 텍스트 한 줄만 표시하던 초기 로딩은 실제 보드와 레이아웃 차이가 커서 기각하고 다섯 컬럼 스켈레톤으로 교체했다.
- 전체 지원자 없음과 현재 필터 결과 없음을 같은 빈 상태로 처리하면 사용자가 다음 행동을 판단하기 어려워 원본 배열과 필터 결과를 별도로 검사했다.
- `#11`은 `#9`의 필터 결과와 연결되어야 하므로 독립 worktree에서 UI 모듈을 먼저 만들고 `#9` 커밋 위에 stacked 방식으로 통합했다.
- 첫 빈 상태 통합 테스트가 최종 응답을 기다리지 않고 초기 로딩 상태를 먼저 선택하는 문제를 발견했다. 최종 빈 상태 문구가 나타날 때까지 기다리도록 테스트를 수정했다.
- 상태 컴포넌트 테스트에서 스켈레톤 구조, 오류·재시도, 재시도 중 비활성화, 두 빈 상태와 필터 초기화를 확인했다. 통합 테스트에서는 GET 503 이후 재시도 성공과 원본·필터 빈 상태 분기를 검증했다.
